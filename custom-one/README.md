# use npm init (enter) to initialise
# use npm install @actions/core --save to install dependancies 
npm install -g @vercel/ncc
ncc --version
make sure const core has he correct path
ncc build index.js --license licenses.txt